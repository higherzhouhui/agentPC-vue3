import { translateTitle } from '@/locale'

const defaultSetting = {
  title: translateTitle('CUST.translate337'),
  showSettings: true,
  tagsView: true,
  fixedHeader: true,
  sidebarLogo: true
}

export default defaultSetting
